{
  "name": "tcl-ac-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js",
    "test": "node --test tests/*.test.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply tcl-ac-state --local",
    "migrate:remote": "wrangler d1 migrations apply tcl-ac-state --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
